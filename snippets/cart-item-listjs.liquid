<ul class="cartitems--list" data-cart-item-list>
  {% for item in cart.items %}
    {%
      render 'cart-itemjs',
      item: item,
      show_sku: show_sku,
      item_index: forloop.index,
    %}
  {% endfor %}
</ul>
